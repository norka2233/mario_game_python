import osfrom pygame import *import blockimport monsterMOVE_SPEED, MOVE_EXTRA_SPEED = 7, 2.5PLAYER_WIDTH, PLAYER_HEIGHT, PLAYER_COLOR = 22, 32, '#888888'POWER_JUMP, POWER_EXTRA_JUMP, GRAVITY = 10, 1, 0.25ANIMATion_DELAY, ANIMATION_SUPER_SPEED_DELAY = 0.1, 0.05ICON_DIR = os.path.dirname(__file__)ANIMATION_LEFT = [ICON_DIR]ANIMATION_RIGHT = [ICON_DIR]ANIMATION_JUMP = [ICON_DIR]ANIMATION_JUMP_LEFT = [ICON_DIR]ANIMATION_JUMP_RIGHT = [ICON_DIR]ANIMATION_STAY = [ICON_DIR]class Player(sprite.Sprite):    def __init__(self):        # sprite.Sprite.__init__(self)        super.__init__(self)    def update(self, left, right, up, run, platform):        if up:            pass        if left:            pass        if right:            pass    def collide(self, x_value, y_value, platform):        pass    def teleport(self, go_x, go_y):        self.rect.x = go_x        self.rect.y = go_y    def die(self):        time.wait(500)        self.teleport(self.start_x, start_y)