import osfrom pygame import *MONSTER_WIDTH = MONSTER_HEIGHT = MONSTER_COLOR = 32, 32, "#2111FF"ICON_FIR = os.path.dirname(__file__)class Monster(sprite.Sprite):    def __init__(self, x, y, left, up, max_length_left, max_length_up ):        pass    def update(self):        pass    def collide(self, platform):        pass